/**
 * Java/API compatibility layer.
 *
 * <p>Place Bukkit or Paper version-sensitive access here to keep domain logic in stable packages.</p>
 *
 * <p>Use {@link com.skyblockexp.ezlifesteal.compat.AdapterSupport} as the shared support entrypoint for
 * scheduler-sensitive and player-bound operations so adapters remain reusable across Paper and versioned Folia
 * modules.</p>
 */
package com.skyblockexp.ezlifesteal.compat;
